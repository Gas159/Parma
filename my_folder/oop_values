переменные внутри класса называются атрибутами или свойтсвами

делегирование - super()
print(NoteBook.__mro__)

n.__sizeof__() + n.__dict_.sizeof() - размер занимаемый обьектом (байт)
__slots__ = ('x', 'y') - ограничение локальных свойст обьекта, уменьшение места и ускорение работы

setattr(Point, 'prop', 1) # add values to class, если нету то добавляем
getattr(Point, 'a', False) # get (берет) значение из класса или экземпляра или возвращает False

hasattr(Point, 'prop') # return bool exist  attr prop

del Point.prop  # delete attr prop
delattr(Point, 'prop')

