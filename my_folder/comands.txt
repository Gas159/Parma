There are not the page you're looking for. Come Home, padawan
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


- requirements.txt
- Procfile
- runtime.txt
- django-project
- environment variables / config variables
- static file configuration
- database configuration
- migrate schema of database
- gitignore file for ignoring virtualenvs, .env file, staticfiles, etc

python manage.py #all commmands for django
django-admin startproject hexlet_django_blog .
python manage.py migrate --run-syncdb # user_users


python manage.py runserver
python manage.py startapp 'name app'
python manage.py createsuperuser
django-admin # list commands


# python manage.py sqlmigrate women 0001  // позволяет посмотреть sql запрос который будет выполнен
# python manage.py makemigrations
# python manage.py migrate
python manage.py migrate article 0005
python manage.py migrate article zero

python manage.py shell
web: python manage.py migrate && gunicorn <project_name>.wsgi

    setUp – выполняется перед запуском каждого теста;
    tearDown – выполняется после завершения каждого теста;
    setUpTestData – выполняется перед запуском всех тестов конкретного класса.


from hexlet_django_blog.article.models import Article # Импортируем наш класс модели Article
a = Article()
# Когда объект модели создан, его можно наполнять данными через обычные свойства.
a.name = 'my super article'
a.body = 'my content'
a # <Article: Article object (None)>
a.id
a.name # 'my super article'
a.body # 'my content'

a = Article.objects.get(id=1)
a = Article.objects.filter(name='another title')
Article.objects.filter(id__gte\lte=50).first().body # 'my content'

a = Article.objects.all()
a # <QuerySet [<Article: Article object (1)>]>

a = Article.objects.get(id=1)
a.delete() # (1, {'article.Article': 1})
Article.objects.all() # <QuerySet []>


view - вьюха или обработчик

echo "# content" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Gas159/gjano_selfedu.git
git push -u origin main


rm -rf .git

git remote -v
git remote remove origin


 <a href="/about">О блоге</a>
      <a href="/articles">Статьи</a>
      <a href="/categories">Категории статей</a>


a = Article()
a.name = '6'
a.name = '66'
a.save()



curl -X POST https://dummyjson.com/users/add \
   -H "Content-Type: application/json" \
   -d '{ "firstName": "Samsa", "age": 100 }'

curl --head -X GET https://code-basics.com
curl http:// www.wedserver.com:8000/
curl -H "content-type: application/json" localhost:8080/courses

# -X, --request - задает метод запроса
# --head в этом случае игнорирует тело при выводе
# -v, --verbose
# -H, --header

postgresql://postgres:l3D70CB9tmU589W8Eh8m@containers-us-west-110.railway.app:5926/railway